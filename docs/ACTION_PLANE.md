# План действий: Завершение разработки приложения HobbyHub

Текущая готовность приложения: **~68%**. Отличный фундамент (Clean Architecture) и интеграции. Основные проблемы: использование хардкод-заглушек в интерфейсе, незавершенные формы создания контента, отсутствие чатов и синтаксическая ошибка, блокирующая сборку.

## Фаза 1: Критические исправления и разблокировка онбординга
1. **Исправить синтаксис AnalyticsService**
   - Файл: `apps/lib/services/firebase/analytics_service.dart` (строка 61).
   - Действие: Заменить `'category': ?category,` на `'category': category,`.
2. **Завершить экран геолокации**
   - Файл: `apps/lib/ui/onboarding/location_screen.dart`.
   - Действие: Интегрировать `Geolocator` для запроса прав и получения координат пользователя.
3. **Имплементировать флоу согласия родителей**
   - Файл: `apps/lib/ui/onboarding/parental_consent_screen.dart`.
   - Действие: Настроить логику для пользователей младше 18 лет.

## Фаза 2: Отказ от моковых данных
1. **Подключить реальный поиск событий**
   - Файл: `apps/lib/ui/search/search_screen.dart`.
   - Действие: Удалить заглушки, использовать `firestoreService.searchEventsByCategories()`.
2. **Настроить стриминг групп**
   - Файл: `apps/lib/ui/communities/communities_screen.dart`.
   - Действие: Убрать захардкоженный список `_myGroups`, подписаться на `firestoreService.userCommunitiesStream()`.
3. **Обновить загрузку интересов профиля**
   - Файл: `apps/lib/ui/profile/profile_screen.dart`.
   - Действие: Считывать данные из `currentUserStreamProvider` без фолбэков на тестовые данные.

## Фаза 3: Завершение ключевых сценариев
1. **Завершить форму создания события**
   - Файл: `apps/lib/ui/create/create_event_screen.dart`.
   - Действие: Добавить финальный `submit` в Firestore и загрузку массива фотографий в Firebase Cloud Storage.
2. **Сделать функциональным детальный экран события**
   - Файл: `apps/lib/ui/home/event_detail_screen.dart`.
   - Действие: Заменить заглушку на полноценный экран, отображающий информацию о событии, его участников и кнопку присоединения.
3. **Интеграция с Google Maps**
   - Действие: Настроить радиус поиска и вывод маркеров (пинов) событий на карте региона.

## Фаза 4: Безопасность и функционал чатов
1. **Защита баз данных (Firestore Rules)**
   - Файл: `infrastructure/firebase/firestore.rules`.
   - Действие: Написать правила для коллекций `notifications`, `messages`, `reports`, `activityHistory`. Добавить проверки минимального возраста (`minAge`).
2. **Разработать систему личных сообщений (MVP)**
   - Действие: Написать UI и логику (через Firestore) обмена сообщениями между пользователями. Создать экраны списка чатов и самой переписки.

## Фаза 5: Тестирование и стабилизация
1. **Вычистить роутер**
   - Файл: `apps/lib/core/router/app_router.dart`.
   - Действие: Удалить неиспользуемые `TODO` и заглушки роутеров.
2. **Обработка ошибок UI**
   - Действие: Добавить снекбары и alert-диалоги для отображения ошибок пользователю (сейчас они глотаются только провайдерами).
3. **Покрытие тестами**
   - Действие: Написать виджет-тесты и модульные тесты для ключевых пользовательских путей (авторизация, создание события, поиск).